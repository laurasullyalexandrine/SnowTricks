<nav class="navbar navbar-expand-lg" style="background-color:#000;">
	<div class="container">
		<div class="d-flex flex-wrap mt-4">
			<a class="navbar-brand text-center" href="{{path('home')}}">
				<img src="/image/snowtricks-logo.png" alt="snotricks-logo">
			</a>
		</div>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon">
				<i class="fas fa-solid fa-bars-staggered"></i>
			</span>
		</button>

		<div class="collapse navbar-collapse col-6" id="navbarColor01">
			<ul class="navbar-nav ms-auto text-center">
				<div class="d-lg-flex flex-lg-row flex-xl-row lex-xxl-row flex-xs-column flex-sm-column">
					<li class="nav-item">
						<a class="nav-link text-light fs-4" href="{{path('home')}}">Accueil</a>
					</li>
				</div>
				{% if app.user == null %}
					<div
						class="d-lg-flex flex-lg-row flex-xl-row lex-xxl-row flex-xs-column flex-sm-column">
						{# s'indentifier #}
						<li class="nav-item">
							<a class="nav-link text-light fs-4" href="{{ path('app_login') }}">Connexion</a>
						</li>
						{# s'inscrire #}
						<li class="nav-item">
							<a class="nav-link text-light fs-4" href="{{ path('app_register')}}">Inscription</a>
						</li>
					</div>
				{% endif %}
			</ul>
				{# se déconnecter #}
				{% if app.user %}
					<ul class="navbar-nav d-flex ms-2">
						<li class="nav-item text-center mb-lg-0 mb-3">
							<a class="logout" href="{{ path('app_logout') }}">Déconnexion</a>
						</li> 
					</ul>
				{% endif %}
		</div>
	</nav>
	<div id="welcome">
		<div class="m-auto py-4">
			{% if app.user and app.user.isVerified == true %}
				<ul class="navbar-nav align-self-center ms-2">
					<li class="nav-item text-light text-center mt-1 mb-lg-0 mb-3">
						<span>Bonjour {{ app.user.name|capitalize }} !</span>
					</li> 
				</ul>
			{% else %}
				<ul class="navbar-nav align-self-center ms-2">
					<li class="nav-item text-light text-center mt-1 mb-lg-0 mb-3">
						<span>Bienvenue !</span>
					</li> 
				</ul>	
			{% endif %}
		</div>
	</div>