<nav class="navbar navbar-expand-lg bg-black bg-dark d-none d-sm-none d-md-block d-lg-block">
	<div class="container">
		<div class="d-flex flex-wrap mt-4">
			<a class="navbar-brand text-center" href="{{path('home')}}">
				<img src="{{ asset('image/snowtricks-logo.png')}}" alt="snotricks-logo">
			</a>
		</div>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon">
				<i class="fas fa-solid fa-bars-staggered"></i>
			</span>
		</button>

		<div class="collapse navbar-collapse col-6" id="navbarColor01">
			<ul class="navbar-nav ms-auto text-center">
				<div class="d-lg-flex flex-lg-row flex-xl-row lex-xxl-row flex-xs-column flex-sm-column">
					<li class="nav-item">
						<a class="nav-link text-light fs-4" href="{{path('home')}}">Accueil</a>
					</li>
				</div>
				{% if app.user == null %}
					<div
						class="d-lg-flex flex-lg-row flex-xl-row lex-xxl-row flex-xs-column flex-sm-column">
						{# s'indentifier #}
						<li class="nav-item">
							<a class="nav-link text-light fs-4" href="{{ path('login') }}">Connexion</a>
						</li>
						{# s'inscrire #}
						<li class="nav-item">
							<a class="nav-link text-light fs-4" href="{{ path('register')}}">Inscription</a>
						</li>
					</div>
				{% endif %}
			</ul>
				{# user logged in #}
				{% if app.user %}
					<ul class="navbar-nav d-flex ms-2">
						<li class="nav-item me-4">
							<a class="nav-link text-light fs-4" href="{{path('comments_user_slug', { user_slug: app.user.slug } )}}">Tricks</a>
						</li>
						<li class="nav-item text-center mb-lg-0 mb-3">
							<a class="logout" href="{{ path('logout') }}">Déconnexion</a>
						</li> 
					</ul>
				{% endif %}
		</div>
	</nav>
